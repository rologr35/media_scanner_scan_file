# media_scanner_scan_file

A new flutter plugin to scan media files and downloaded files using MediaScanner.

## Getting Started

This plugin allows you to scan media files in android file system so system imports them to MediaStore tables using MediaScanner.

